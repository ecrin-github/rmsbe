using rmsbe.DbModels;
using rmsbe.SysModels;

namespace rmsbe.SysModels;

public class DataMapper 
{
    /*
    public ICollection<Dta> DtaDtoBuilder(ICollection<DtaInDb> dtas)
    {
        return dtas is not { Count: > 0 } ? null : dtas.Select(DtaDtoMapper).ToList();
    }

    public ICollection<Dtp> DtpDtoBuilder(ICollection<DtpInDb> dtps)
    {
        return dtps is not { Count: > 0 } ? null : dtps.Select(DtpDtoMapper).ToList();
    }

    public ICollection<DtpDataset> DtpDatasetDtoBuilder(ICollection<DtpDatasetInDb> dtpDatasets)
    {
        return dtpDatasets is not { Count: > 0 } ? null : dtpDatasets.Select(DtpDatasetDtoMapper).ToList();
    }

    public ICollection<DtpObject> DtpObjectDtoBuilder(ICollection<DtpObjectInDb> dtpObjects)
    {
        return dtpObjects is not { Count: > 0 } ? null : dtpObjects.Select(DtpObjectDtoMapper).ToList();
    }

    public ICollection<DtpStudy> DtpStudyDtoBuilder(ICollection<DtpStudyInDb> dtpStudies)
    {
        return dtpStudies is not { Count: > 0 } ? null : dtpStudies.Select(DtpStudyDtoMapper).ToList();
    }

    public ICollection<Dua> DuaDtoBuilder(ICollection<DuaInDb> duas)
    {
        return duas is not { Count: > 0 } ? null : duas.Select(DuaDtoMapper).ToList();
    }

    public ICollection<Dup> DupDtoBuilder(ICollection<DupInDb> dups)
    {
        return dups is not { Count: > 0 } ? null : dups.Select(DupDtoMapper).ToList();
    }

    public ICollection<DupObject> DupObjectDtoBuilder(ICollection<DupObjectInDb> dupObjects)
    {
        return dupObjects is not { Count: > 0 } ? null : dupObjects.Select(DupObjectDtoMapper).ToList();
    }

    public ICollection<DupPrereq> DupPrereqDtoBuilder(ICollection<DupPreReqInDb> dupPrereqs)
    {
        return dupPrereqs is not { Count: > 0 } ? null : dupPrereqs.Select(DupPrereqDtoMapper).ToList();
    }

    public ICollection<SecondaryUse> SecondaryUseDtoBuilder(ICollection<SecondaryUseInDb> secondaryUses)
    {
        return secondaryUses is not { Count: > 0 } ? null : secondaryUses.Select(SecondaryUseDtoMapper).ToList();
    }

    public ICollection<AccessPrereq> AccessPrereqDtoBuilder(ICollection<AccessPrereqInDb> accessPrereq)
    {
        return accessPrereq is not { Count: > 0 } ? null : accessPrereq.Select(AccessPrereqDtoMapper).ToList();
    }

    public ICollection<ProcessNote> ProcessNoteDtoBuilder(ICollection<ProcessNoteInDb> processNotes)
    {
        return processNotes is not { Count: > 0 } ? null : processNotes.Select(ProcessNoteDtoMapper).ToList();
    }

    public ICollection<ProcessPeople> ProcessPeopleDtoBuilder(ICollection<ProcessPeopleInDb> processPeople)
    {
        return processPeople is not { Count: > 0 } ? null : processPeople.Select(ProcessPeopleDtoMapper).ToList();
    }
    */
}   

